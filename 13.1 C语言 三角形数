#include<stdio.h>
int main()
{
    int a[50],i,b=1;
    for(i=0;i<50;i++)
    {
        a[i]=b;
        b=b+i+2;
    }
    for(i=0;i<50;i++)
    {
        if((i+1)%10==0&i!=49)
		printf("%d\n",a[i]);
		else if(i==49)
		printf("%d",a[i]);
		else
		printf("%d ",a[i]);
    }
    return 0;
}
